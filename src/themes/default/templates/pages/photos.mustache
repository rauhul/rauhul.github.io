{{<html}}
{{$main}}
    <div id="photo-grid">
        {{#photos}}
        <div class="photo-item">
            <img src="{{thumbnail-url}}" alt="{{description}}" loading=lazy class="photo-img">
        </div>
        {{/photos}}
    </div>
{{/main}}
{{/html}}
